---
summary: '当需要记录KPI 跟踪，并梳理指标定义/口径、当前值与数据源、目标/阈值、趋势与状态时使用。'
tags: [journal, template, hr-operations, kpi-tracking]
sources: []
---

# KPI 跟踪

> 适用：用于整理KPI 跟踪；只写来源支持的事实，并显式保留未知项。

<StatGroup>
  <Stat label="当前值" value="待填" />
  <Stat label="目标值" value="待填" />
  <Stat label="偏差" value="待填" />
</StatGroup>

当前状态：<StatusBadge status="待确认" tone="neutral" />

<Table
  headers={['异常或阻塞', '影响', '动作']}
  rows={[
    [
      '【填写趋势与状态；未知时写“待确认”，不要推断】',
      '【填写影响；未知时写“待确认”，不要推断】',
      '【填写偏差解释/驱动因素；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

## 记录提示

### 指标定义/口径

【填写指标定义/口径；未知时写“待确认”，不要推断】

### 当前值与数据源

【填写当前值与数据源；未知时写“待确认”，不要推断】

### 目标/阈值

【填写目标/阈值；未知时写“待确认”，不要推断】

### 趋势与状态

【填写趋势与状态；未知时写“待确认”，不要推断】

### 偏差解释/驱动因素

【填写偏差解释/驱动因素；未知时写“待确认”，不要推断】

### 调整动作

【填写调整动作；未知时写“待确认”，不要推断】

### 复盘 cadence 与指标负责人

【填写复盘 cadence 与指标负责人；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '每个 KPI 都有口径、数据源、负责人和目标阈值', state: 'todo' },
    { text: '每个 off-target 指标都有偏差解释、调整动作和截止时间', state: 'todo' },
    { text: '记录中包含上次状态、本次状态变化和下一次复盘日期', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
