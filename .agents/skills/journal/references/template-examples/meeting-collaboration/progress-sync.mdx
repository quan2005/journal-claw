---
summary: '当需要记录进展同步纪要，并梳理轨道/负责人、上次承诺 vs 当前状态、状态变化、当前约束时使用。'
tags: [journal, template, meeting-collaboration, progress-sync]
sources: []
---

# 进展同步纪要

> 适用：用于整理进展同步纪要；只写来源支持的事实，并显式保留未知项。

<StatGroup>
  <Stat label="完成项" value="待填" />
  <Stat label="进行中" value="待填" />
  <Stat label="阻塞" value="待填" />
</StatGroup>

<Table
  headers={['事项', '状态变化', '阻塞', '负责人']}
  rows={[
    [
      '【填写轨道/负责人；未知时写“待确认”，不要推断】',
      '【填写本次变化；未知时写“待确认”，不要推断】',
      '【填写状态变化；未知时写“待确认”，不要推断】',
      '【填写负责人；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

<Steps heading="下一步" items={[{ title: '【填写当前约束；未知时写“待确认”，不要推断】' }]} />

## 记录提示

### 轨道/负责人

【填写轨道/负责人；未知时写“待确认”，不要推断】

### 上次承诺 vs 当前状态

【填写上次承诺 vs 当前状态；未知时写“待确认”，不要推断】

### 状态变化

【填写状态变化；未知时写“待确认”，不要推断】

### 当前约束

【填写当前约束；未知时写“待确认”，不要推断】

### 需要协助

【填写需要协助；未知时写“待确认”，不要推断】

### 下次可验证结果

【填写下次可验证结果；未知时写“待确认”，不要推断】

### 风险/阻塞板

【填写风险/阻塞板；未知时写“待确认”，不要推断】

### 行动项

【填写行动项；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '每个轨道都有相对上次同步的状态变化', state: 'todo' },
    { text: '每个阻塞都写清影响、解除负责人和截止时间', state: 'todo' },
    { text: '下一步计划是可验证结果，不能只写继续推进', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
