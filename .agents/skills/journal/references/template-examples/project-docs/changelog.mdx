---
summary: '当需要记录变更日志，并梳理版本、发布日期、新增、变更时使用。'
tags: [journal, template, project-docs, changelog]
sources: []
---

# 变更日志

> 适用：用于整理变更日志；只写来源支持的事实，并显式保留未知项。

<Summary
  title="【填写版本；未知时写“待确认”，不要推断】"
  body="【填写本次最重要的变化；未知时写“待确认”，不要推断】"
/>

<Table
  headers={['变化', '影响对象', '兼容性或迁移动作']}
  rows={[
    [
      '【填写发布日期；未知时写“待确认”，不要推断】',
      '【填写影响对象；未知时写“待确认”，不要推断】',
      '【填写修复；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

<Checklist
  items={[
    { text: '升级说明已验证', state: 'todo' },
    { text: '受影响方已通知', state: 'todo' },
  ]}
/>

## 记录提示

### 版本

【填写版本；未知时写“待确认”，不要推断】

### 发布日期

【填写发布日期；未知时写“待确认”，不要推断】

### 新增

【填写新增；未知时写“待确认”，不要推断】

### 变更

【填写变更；未知时写“待确认”，不要推断】

### 修复

【填写修复；未知时写“待确认”，不要推断】

### Breaking changes

【填写Breaking changes；未知时写“待确认”，不要推断】

### 用户/系统影响

【填写用户/系统影响；未知时写“待确认”，不要推断】

### 迁移/兼容性

【填写迁移/兼容性；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '升级说明已验证', state: 'todo' },
    { text: '受影响方已通知', state: 'todo' },
    { text: '条目必须按新增、变更、修复、破坏性变更分组', state: 'todo' },
    { text: '破坏性变更必须写迁移动作', state: 'todo' },
    { text: '每条重要变更必须有来源或 issue/PR 编号', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
