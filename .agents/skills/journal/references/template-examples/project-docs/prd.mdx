---
summary: '当需要记录产品需求文档，并梳理目标用户、核心场景/JTBD、用户问题、非目标场景时使用。'
tags: [journal, template, project-docs, prd]
sources: []
---

# 产品需求文档

> 适用：用于整理产品需求文档；只写来源支持的事实，并显式保留未知项。

<DecisionRecord
  question="【填写本次需要确定的需求边界；未知时写“待确认”，不要推断】"
  decision="【填写当前边界；未知时写“待确认”，不要推断】"
  rationale="【填写用户证据和取舍；未知时写“待确认”，不要推断】"
/>

<Table
  headers={['场景', '需求', '优先级', '验收标准', '来源']}
  rows={[
    [
      '【填写核心场景/JTBD；未知时写“待确认”，不要推断】',
      '【填写非目标场景；未知时写“待确认”，不要推断】',
      '【P0 / P1 / P2】',
      '【填写验收标准；未知时写“待确认”，不要推断】',
      '【填写成功指标；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

## 记录提示

### 目标用户

【填写目标用户；未知时写“待确认”，不要推断】

### 核心场景/JTBD

【填写核心场景/JTBD；未知时写“待确认”，不要推断】

### 用户问题

【填写用户问题；未知时写“待确认”，不要推断】

### 非目标场景

【填写非目标场景；未知时写“待确认”，不要推断】

### 可测试需求

【填写可测试需求；未知时写“待确认”，不要推断】

### 验收标准

【填写验收标准；未知时写“待确认”，不要推断】

### 成功指标

【填写成功指标；未知时写“待确认”，不要推断】

### 开放问题

【填写开放问题；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '每条需求必须对应用户问题或场景', state: 'todo' },
    { text: '每条验收标准必须可测试', state: 'todo' },
    { text: '成功指标必须写基线或目标值', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
