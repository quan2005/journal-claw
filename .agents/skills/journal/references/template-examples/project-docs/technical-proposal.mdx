---
summary: '当需要记录技术方案，并梳理问题、不可变约束、方案选项、架构草图/数据流时使用。'
tags: [journal, template, project-docs, technical-proposal]
sources: []
---

# 技术方案

> 适用：用于整理技术方案；只写来源支持的事实，并显式保留未知项。

<Mermaid
  chart={'flowchart LR\n  A[当前系统] --> B[拟议变更]\n  B --> C[验证与回滚]'}
  caption="只保留影响决策的系统边界"
/>

<ComparisonMatrix
  columns={['复杂度', '兼容性', '运维风险']}
  rows={[{ label: '候选方案', values: ['【待评估】', '【待评估】', '【待评估】'] }]}
/>

<DecisionRecord
  question="【填写问题；未知时写“待确认”，不要推断】"
  decision="【填写决策状态；未知时写“待确认”，不要推断】"
  rationale="【填写约束与权衡；未知时写“待确认”，不要推断】"
/>

## 记录提示

### 问题

【填写问题；未知时写“待确认”，不要推断】

### 不可变约束

【填写不可变约束；未知时写“待确认”，不要推断】

### 方案选项

【填写方案选项；未知时写“待确认”，不要推断】

### 架构草图/数据流

【填写架构草图/数据流；未知时写“待确认”，不要推断】

### 决策状态

【填写决策状态；未知时写“待确认”，不要推断】

### 后果/权衡

【填写后果/权衡；未知时写“待确认”，不要推断】

### 验证计划

【填写验证计划；未知时写“待确认”，不要推断】

### 回滚方案

【填写回滚方案；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '至少两个方案必须写出取舍', state: 'todo' },
    { text: '最终决策必须有负责人和状态', state: 'todo' },
    { text: '上线计划必须同时写验证方式和回滚触发条件', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
