---
summary: '当需要记录测试计划，并梳理测试目标、测试范围、不测范围、风险模型时使用。'
tags: [journal, template, project-docs, test-plan]
sources: []
---

# 测试计划

> 适用：用于整理测试计划；只写来源支持的事实，并显式保留未知项。

<Steps
  heading="执行顺序"
  items={[
    { title: '【填写不测范围；未知时写“待确认”，不要推断】' },
    { title: '【填写测试数据；未知时写“待确认”，不要推断】' },
    { title: '【填写风险驱动用例；未知时写“待确认”，不要推断】' },
  ]}
/>

<Checklist
  heading="质量门"
  items={[
    { text: '关键路径已验证', state: 'todo' },
    { text: '回滚条件明确', state: 'todo' },
  ]}
/>

## 记录提示

### 测试目标

【填写测试目标；未知时写“待确认”，不要推断】

### 测试范围

【填写测试范围；未知时写“待确认”，不要推断】

### 不测范围

【填写不测范围；未知时写“待确认”，不要推断】

### 风险模型

【填写风险模型；未知时写“待确认”，不要推断】

### 测试数据

【填写测试数据；未知时写“待确认”，不要推断】

### 风险驱动用例

【填写风险驱动用例；未知时写“待确认”，不要推断】

### 入口/退出标准

【填写入口/退出标准；未知时写“待确认”，不要推断】

### 缺陷分级

【填写缺陷分级；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '关键路径已验证', state: 'todo' },
    { text: '回滚条件明确', state: 'todo' },
    { text: '每个测试用例必须映射到需求或风险', state: 'todo' },
    { text: '环境和测试数据必须可复现', state: 'todo' },
    { text: '通过/失败标准必须避免主观词', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
