---
summary: '当需要记录用户故事，并梳理用户角色、触发情境、前置条件、待完成任务时使用。'
tags: [journal, template, project-docs, user-story]
sources: []
---

# 用户故事

> 适用：用于整理用户故事；只写来源支持的事实，并显式保留未知项。

<DecisionRecord
  question="【填写本次需要确定的需求边界；未知时写“待确认”，不要推断】"
  decision="【填写当前边界；未知时写“待确认”，不要推断】"
  rationale="【填写用户证据和取舍；未知时写“待确认”，不要推断】"
/>

<Table
  headers={['场景', '需求', '优先级', '验收标准', '来源']}
  rows={[
    [
      '【填写触发情境；未知时写“待确认”，不要推断】',
      '【填写待完成任务；未知时写“待确认”，不要推断】',
      '【P0 / P1 / P2】',
      '【填写主流程；未知时写“待确认”，不要推断】',
      '【填写例外/边界；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

## 记录提示

### 用户角色

【填写用户角色；未知时写“待确认”，不要推断】

### 触发情境

【填写触发情境；未知时写“待确认”，不要推断】

### 前置条件

【填写前置条件；未知时写“待确认”，不要推断】

### 待完成任务

【填写待完成任务；未知时写“待确认”，不要推断】

### 用户价值/业务价值

【填写用户价值/业务价值；未知时写“待确认”，不要推断】

### 主流程

【填写主流程；未知时写“待确认”，不要推断】

### 例外/边界

【填写例外/边界；未知时写“待确认”，不要推断】

### 验收示例

【填写验收示例；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '每条故事只能服务一个明确用户角色', state: 'todo' },
    { text: '验收条件必须包含 Given/When/Then 或等价可测描述', state: 'todo' },
    { text: '故事必须写清用户价值，不只写系统动作', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
