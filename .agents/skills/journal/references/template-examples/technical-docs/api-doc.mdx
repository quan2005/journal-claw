---
summary: '当需要记录API 文档，并梳理方法与路径、认证方式与权限范围、参数表、请求示例时使用。'
tags: [journal, template, technical-docs, api-doc]
sources: []
---

# API 文档

> 适用：用于整理API 文档；只写来源支持的事实，并显式保留未知项。

<Table
  headers={['参数', '类型', '必填', '说明']}
  rows={[
    [
      '【填写参数；未知时写“待确认”，不要推断】',
      '【类型】',
      '【是 / 否】',
      '【填写说明；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

<Steps
  heading="调用顺序"
  items={[{ title: '准备鉴权' }, { title: '发送请求' }, { title: '处理成功和错误响应' }]}
/>

<CopyButton text="curl 【填写完整且已验证的请求】" label="复制请求" />

## 记录提示

### 端点

【填写端点；未知时写“待确认”，不要推断】

### 鉴权

【填写鉴权；未知时写“待确认”，不要推断】

### 参数

【填写参数；未知时写“待确认”，不要推断】

### 请求

【填写请求；未知时写“待确认”，不要推断】

### 响应

【填写响应；未知时写“待确认”，不要推断】

### 错误

【填写错误；未知时写“待确认”，不要推断】

### 示例

【填写示例；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '请求示例和响应示例能直接复制运行', state: 'todo' },
    { text: '每个错误码都写明状态、原因、是否可重试和调用方动作', state: 'todo' },
    { text: '兼容性章节写清新增字段、废弃字段、破坏性变更和旧客户端风险', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
