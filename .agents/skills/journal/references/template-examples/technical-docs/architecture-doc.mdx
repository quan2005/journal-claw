---
summary: '当需要记录架构文档，并梳理系统上下文与外部依赖、C4 层级、需求、质量属性时使用。'
tags: [journal, template, technical-docs, architecture-doc]
sources: []
---

# 架构文档

> 适用：用于整理架构文档；只写来源支持的事实，并显式保留未知项。

<Mermaid
  chart={'flowchart LR\n  Client[调用方] --> Boundary[系统边界]\n  Boundary --> Store[状态或数据]'}
  caption="替换为真实架构和数据流"
/>

<ComparisonMatrix
  columns={['复杂度', '兼容性', '可运维性']}
  rows={[
    { label: '方案 A', values: ['【待评估】', '【待评估】', '【待评估】'] },
    { label: '方案 B', values: ['【待评估】', '【待评估】', '【待评估】'] },
  ]}
/>

<DecisionRecord
  question="【填写系统上下文与外部依赖；未知时写“待确认”，不要推断】"
  decision="【填写拟议方案；未知时写“待确认”，不要推断】"
  rationale="【填写关键失败模式；未知时写“待确认”，不要推断】"
/>

## 记录提示

### 系统上下文与外部依赖

【填写系统上下文与外部依赖；未知时写“待确认”，不要推断】

### C4 层级

【填写C4 层级；未知时写“待确认”，不要推断】

### 需求

【填写需求；未知时写“待确认”，不要推断】

### 质量属性

【填写质量属性；未知时写“待确认”，不要推断】

### 依赖方向

【填写依赖方向；未知时写“待确认”，不要推断】

### 关键失败模式

【填写关键失败模式；未知时写“待确认”，不要推断】

### 架构决策

【填写架构决策；未知时写“待确认”，不要推断】

### 未来演进约束

【填写未来演进约束；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '图中每个外部系统、数据库、队列或第三方服务都有职责说明和失败影响', state: 'todo' },
    { text: '至少列出 3 个质量属性及验证方式', state: 'todo' },
    { text: '兼容性与迁移段落写明旧模块、旧数据、旧 API 或旧客户端影响', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
