---
summary: '当需要记录代码评审记录，并梳理变更文件与职责、发现、严重度、证据时使用。'
tags: [journal, template, technical-docs, code-review]
sources: []
---

# 代码评审记录

> 适用：用于整理代码评审记录；只写来源支持的事实，并显式保留未知项。

<Callout tone="warning" title="阻塞问题">
  【填写最高严重度发现；没有时写“无”；未知时写“待确认”，不要推断】
</Callout>

<Table
  headers={['严重度', '文件或范围', '发现', '建议']}
  rows={[
    [
      'P1',
      '【填写变更文件与职责；未知时写“待确认”，不要推断】',
      '【填写严重度；未知时写“待确认”，不要推断】',
      '【填写修复建议；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

<Checklist
  items={[
    { text: '行为回归有测试覆盖', state: 'todo' },
    { text: '剩余风险已说明', state: 'todo' },
  ]}
/>

## 记录提示

### 变更文件与职责

【填写变更文件与职责；未知时写“待确认”，不要推断】

### 发现

【填写发现；未知时写“待确认”，不要推断】

### 严重度

【填写严重度；未知时写“待确认”，不要推断】

### 证据

【填写证据；未知时写“待确认”，不要推断】

### 行为变化与回归风险

【填写行为变化与回归风险；未知时写“待确认”，不要推断】

### 必改项

【填写必改项；未知时写“待确认”，不要推断】

### 已有/缺失测试

【填写已有/缺失测试；未知时写“待确认”，不要推断】

### 兼容性影响

【填写兼容性影响；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '行为回归有测试覆盖', state: 'todo' },
    { text: '剩余风险已说明', state: 'todo' },
    { text: '每条发现都指向具体文件、函数、测试、日志或用户行为风险', state: 'todo' },
    { text: '高/中风险项必须写明缺失测试或需要补的验证命令', state: 'todo' },
    { text: '兼容性检查明确覆盖旧数据、旧配置、旧调用方或跨平台行为', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
