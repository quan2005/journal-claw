---
summary: '当需要记录部署 Runbook，并梳理环境与前置条件、影响范围和冻结窗口、部署命令与参数、部署前验证时使用。'
tags: [journal, template, technical-docs, deployment-runbook]
sources: []
---

# 部署 Runbook

> 适用：用于整理部署 Runbook；只写来源支持的事实，并显式保留未知项。

<Callout tone="warning" title="执行边界">
  【填写影响范围和冻结窗口；未知时写“待确认”，不要推断】
</Callout>

<Steps
  heading="执行步骤"
  items={[
    { title: '【填写部署前验证；未知时写“待确认”，不要推断】' },
    { title: '【填写部署后验证；未知时写“待确认”，不要推断】' },
    { title: '【填写负责人和值班升级路径；未知时写“待确认”，不要推断】' },
  ]}
/>

<Checklist
  items={[
    { text: '前置条件满足', state: 'todo' },
    { text: '回滚路径可执行', state: 'todo' },
  ]}
/>

## 记录提示

### 环境与前置条件

【填写环境与前置条件；未知时写“待确认”，不要推断】

### 影响范围和冻结窗口

【填写影响范围和冻结窗口；未知时写“待确认”，不要推断】

### 部署命令与参数

【填写部署命令与参数；未知时写“待确认”，不要推断】

### 部署前验证

【填写部署前验证；未知时写“待确认”，不要推断】

### 部署后验证

【填写部署后验证；未知时写“待确认”，不要推断】

### 回滚步骤与触发条件

【填写回滚步骤与触发条件；未知时写“待确认”，不要推断】

### 负责人和值班升级路径

【填写负责人和值班升级路径；未知时写“待确认”，不要推断】

### 风险

【填写风险；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '前置条件满足', state: 'todo' },
    { text: '回滚路径可执行', state: 'todo' },
    { text: '每条命令都包含目标环境、必要变量、预期输出或成功判断', state: 'todo' },
    { text: '验证步骤覆盖部署前、部署后和回滚后三个阶段', state: 'todo' },
    { text: '回滚触发条件可观察', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
