---
summary: '当需要记录迁移指南，并梳理迁移前状态、迁移后状态、分阶段步骤、兼容窗口和受影响方时使用。'
tags: [journal, template, technical-docs, migration-guide]
sources: []
---

# 迁移指南

> 适用：用于整理迁移指南；只写来源支持的事实，并显式保留未知项。

<Callout tone="warning" title="执行边界">
  【填写迁移后状态；未知时写“待确认”，不要推断】
</Callout>

<Steps
  heading="执行步骤"
  items={[
    { title: '【填写兼容窗口和受影响方；未知时写“待确认”，不要推断】' },
    { title: '【填写数据备份/校验/恢复；未知时写“待确认”，不要推断】' },
    { title: '【填写回滚或前滚策略；未知时写“待确认”，不要推断】' },
  ]}
/>

<Checklist
  items={[
    { text: '前置条件满足', state: 'todo' },
    { text: '回滚路径可执行', state: 'todo' },
  ]}
/>

## 记录提示

### 迁移前状态

【填写迁移前状态；未知时写“待确认”，不要推断】

### 迁移后状态

【填写迁移后状态；未知时写“待确认”，不要推断】

### 分阶段步骤

【填写分阶段步骤；未知时写“待确认”，不要推断】

### 兼容窗口和受影响方

【填写兼容窗口和受影响方；未知时写“待确认”，不要推断】

### 数据备份/校验/恢复

【填写数据备份/校验/恢复；未知时写“待确认”，不要推断】

### 切换/灰度/冻结窗口

【填写切换/灰度/冻结窗口；未知时写“待确认”，不要推断】

### 回滚或前滚策略

【填写回滚或前滚策略；未知时写“待确认”，不要推断】

### 验证

【填写验证；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '前置条件满足', state: 'todo' },
    { text: '回滚路径可执行', state: 'todo' },
    { text: '迁移前后差异能对照检查', state: 'todo' },
    { text: '每个阶段都有验证命令、预期结果和失败处理方式', state: 'todo' },
    { text: '回滚策略说明哪些变化可回滚、哪些只能前滚，以及数据安全风险', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
