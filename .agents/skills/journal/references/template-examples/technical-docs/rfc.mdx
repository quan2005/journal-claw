---
summary: '当需要记录RFC，并梳理问题陈述与成功标准、提案概要、替代方案与拒绝理由、决策状态时使用。'
tags: [journal, template, technical-docs, rfc]
sources: []
---

# RFC

> 适用：用于整理RFC；只写来源支持的事实，并显式保留未知项。

<Mermaid
  chart={'flowchart LR\n  Client[调用方] --> Boundary[系统边界]\n  Boundary --> Store[状态或数据]'}
  caption="替换为真实架构和数据流"
/>

<ComparisonMatrix
  columns={['复杂度', '兼容性', '可运维性']}
  rows={[
    { label: '方案 A', values: ['【待评估】', '【待评估】', '【待评估】'] },
    { label: '方案 B', values: ['【待评估】', '【待评估】', '【待评估】'] },
  ]}
/>

<DecisionRecord
  question="【填写问题陈述与成功标准；未知时写“待确认”，不要推断】"
  decision="【填写拟议方案；未知时写“待确认”，不要推断】"
  rationale="【填写迁移/灰度/回滚；未知时写“待确认”，不要推断】"
/>

## 记录提示

### 问题陈述与成功标准

【填写问题陈述与成功标准；未知时写“待确认”，不要推断】

### 提案概要

【填写提案概要；未知时写“待确认”，不要推断】

### 替代方案与拒绝理由

【填写替代方案与拒绝理由；未知时写“待确认”，不要推断】

### 决策状态

【填写决策状态；未知时写“待确认”，不要推断】

### 兼容性契约

【填写兼容性契约；未知时写“待确认”，不要推断】

### 迁移/灰度/回滚

【填写迁移/灰度/回滚；未知时写“待确认”，不要推断】

### 开放问题

【填写开放问题；未知时写“待确认”，不要推断】

### 审议记录

【填写审议记录；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '至少列出 2 个替代方案并说明为什么不选', state: 'todo' },
    { text: '兼容性章节明确列出受影响 API、数据、配置、依赖方或运行环境', state: 'todo' },
    { text: '开放问题必须标注负责人、决策截止时间和阻塞影响', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
