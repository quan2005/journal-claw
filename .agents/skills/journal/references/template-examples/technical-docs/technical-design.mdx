---
summary: '当需要记录技术设计，并梳理问题背景与触发原因、功能需求与非功能需求、明确不解决什么、已确定/待确认/废弃方案时使用。'
tags: [journal, template, technical-docs, technical-design]
sources: []
---

# 技术设计

> 适用：用于整理技术设计；只写来源支持的事实，并显式保留未知项。

<Mermaid
  chart={'flowchart LR\n  Client[调用方] --> Boundary[系统边界]\n  Boundary --> Store[状态或数据]'}
  caption="替换为真实架构和数据流"
/>

<ComparisonMatrix
  columns={['复杂度', '兼容性', '可运维性']}
  rows={[
    { label: '方案 A', values: ['【待评估】', '【待评估】', '【待评估】'] },
    { label: '方案 B', values: ['【待评估】', '【待评估】', '【待评估】'] },
  ]}
/>

<DecisionRecord
  question="【填写问题背景与触发原因；未知时写“待确认”，不要推断】"
  decision="【填写拟议方案；未知时写“待确认”，不要推断】"
  rationale="【填写验证计划；未知时写“待确认”，不要推断】"
/>

## 记录提示

### 问题背景与触发原因

【填写问题背景与触发原因；未知时写“待确认”，不要推断】

### 功能需求与非功能需求

【填写功能需求与非功能需求；未知时写“待确认”，不要推断】

### 明确不解决什么

【填写明确不解决什么；未知时写“待确认”，不要推断】

### 已确定/待确认/废弃方案

【填写已确定/待确认/废弃方案；未知时写“待确认”，不要推断】

### 核心设计选择

【填写核心设计选择；未知时写“待确认”，不要推断】

### 验证计划

【填写验证计划；未知时写“待确认”，不要推断】

### 回滚或降级路径

【填写回滚或降级路径；未知时写“待确认”，不要推断】

### 未覆盖风险

【填写未覆盖风险；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '每个关键设计选择都有证据来源、约束来源或业务需求来源', state: 'todo' },
    { text: '验证计划包含可执行检查', state: 'todo' },
    { text: '回滚路径写清触发条件、负责人和仍未覆盖的兼容性风险', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
