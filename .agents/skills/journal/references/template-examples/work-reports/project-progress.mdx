---
summary: '当需要记录项目进展汇报，并梳理里程碑验收状态、最新进展、验收证据、关键路径变化时使用。'
tags: [journal, template, work-reports, project-progress]
sources: []
---

# 项目进展汇报

> 适用：用于整理项目进展汇报；只写来源支持的事实，并显式保留未知项。

<MilestoneTimeline
  items={[
    {
      time: '【日期】',
      title: '【填写里程碑验收状态；未知时写“待确认”，不要推断】',
      desc: '【填写里程碑验收状态；未知时写“待确认”，不要推断】',
    },
  ]}
/>

整体状态：<StatusBadge status="待评估" tone="neutral" />

<Table
  headers={['风险或依赖', '影响', '所需支持']}
  rows={[
    [
      '【填写依赖；未知时写“待确认”，不要推断】',
      '【填写影响；未知时写“待确认”，不要推断】',
      '【填写需要支持；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

## 记录提示

### 里程碑验收状态

【填写里程碑验收状态；未知时写“待确认”，不要推断】

### 最新进展

【填写最新进展；未知时写“待确认”，不要推断】

### 验收证据

【填写验收证据；未知时写“待确认”，不要推断】

### 关键路径变化

【填写关键路径变化；未知时写“待确认”，不要推断】

### 范围变化

【填写范围变化；未知时写“待确认”，不要推断】

### 依赖

【填写依赖；未知时写“待确认”，不要推断】

### 风险

【填写风险；未知时写“待确认”，不要推断】

### 需要支持

【填写需要支持；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '每个里程碑必须有截止时间、状态和验收证据', state: 'todo' },
    { text: '每个依赖必须有外部负责人和期望交付时间', state: 'todo' },
    { text: '任何范围变化必须说明对时间、成本或质量的影响', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
