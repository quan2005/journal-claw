---
summary: '当需要记录状态汇报，并梳理当前健康状态、上次状态、状态变化、系统约束时使用。'
tags: [journal, template, work-reports, status-report]
sources: []
---

# 状态汇报

> 适用：用于整理状态汇报；只写来源支持的事实，并显式保留未知项。

<StatGroup>
  <Stat label="当前值" value="待填" />
  <Stat label="目标值" value="待填" />
  <Stat label="偏差" value="待填" />
</StatGroup>

当前状态：<StatusBadge status="待确认" tone="neutral" />

<Table
  headers={['异常或阻塞', '影响', '动作']}
  rows={[
    [
      '【填写系统约束；未知时写“待确认”，不要推断】',
      '【填写影响；未知时写“待确认”，不要推断】',
      '【填写风险；未知时写“待确认”，不要推断】',
    ],
  ]}
/>

## 记录提示

### 当前健康状态

【填写当前健康状态；未知时写“待确认”，不要推断】

### 上次状态

【填写上次状态；未知时写“待确认”，不要推断】

### 状态变化

【填写状态变化；未知时写“待确认”，不要推断】

### 系统约束

【填写系统约束；未知时写“待确认”，不要推断】

### 风险

【填写风险；未知时写“待确认”，不要推断】

### 解除约束所需支持

【填写解除约束所需支持；未知时写“待确认”，不要推断】

### 下一步

【填写下一步；未知时写“待确认”，不要推断】

## 质量检查

<Checklist
  items={[
    { text: '必须同时出现上次状态和当前状态', state: 'todo' },
    { text: '每个状态变化必须写明触发原因或证据', state: 'todo' },
    { text: '每个支持请求必须有支持对象、截止时间和预期结果', state: 'todo' },
  ]}
/>

## 来源

把真实材料路径写入 frontmatter 的 `sources`。没有来源时保留空数组，不要创建看似真实的文件名。
