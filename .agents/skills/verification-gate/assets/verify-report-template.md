---
spec: ./spec.md
date: {YYYY-MM-DD}
round: 1
result: fail         # pass | fail（六项全过才是 pass）
scope: "{核对范围：diff 命令或文件清单描述}"
---

# 验收报告 — {spec 标题}

## AC 核对（不漏 / 不偏）

| AC | 结论 | 证据 |
|---|---|---|
| AC-1 | ✅ pass | `src/foo.ts:42` — 实现了 X，测试 `test/foo.test.ts:10` 通过 |
| AC-2 | ❌ fail | 预期 Y，实际 Z（`src/bar.ts:17`） |

## 范围完整性（不少）

<!-- spec 第 3 节逐条对照；AC 未显式覆盖的范围条目也在此核对 -->

## 非目标越界检查（不多）

<!-- diff 中无法归属到 AC / 范围 / 必要基础设施的改动逐项列出；命中非目标的单独标注 -->

- ✅ / ❌ 说明

## 冗余与均衡（不重 / 不倚）

<!-- 同一 AC 的重复实现；TODO / stub / 静默降级的半成品 AC -->

## 结论

<!-- 全部通过 / 存在偏差：fail 项的修复建议，按风险排序 -->

## 待用户裁决

<!-- 灰色地带：轻微越界、与 AC 字面有出入但疑似更合理的实现。写清两边代价。用户接受的项须回写进 spec 后方可计入通过 -->
