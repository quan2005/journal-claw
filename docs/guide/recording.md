---
title: 语音录音
description: 使用 JournalClaw 录音功能，了解三种语音引擎的选择与差异。
---

# 语音录音

语音录音是谨迹最核心的输入方式。一键开始，AI 接管后续所有处理。

## 操作流程

1. 点击底部 Command Dock 中央的琥珀色录音按钮
2. 说话——会议总结、灵感记录、任务备忘
3. 再次点击停止录音

谨迹自动执行以下处理管线：

```
原始音频采集（cpal + WAV）
  → 降噪（nnnoiseless）
  → 去静默 + 重采样（rubato）
  → 编码为 M4A（afconvert）
  → 语音转文字（STT 引擎）
  → AI 结构化编译
  → 写入知识条目
```

## 录音按钮状态

| 状态 | 颜色 | 说明 |
|---|---|---|
| 待机 | 琥珀金 `#B8782A` / `#C8933B` | 就绪，点击开始录音 |
| 录制中 | 录音红 `#ff3b30` / `#ff375f` | 脉冲动画，正在捕获音频 |
| 处理中 | 琥珀金（AI 状态标识） | 音频正在转写和编译 |

红色是谨迹唯一的"紧急色"，仅在录音中激活——语义精确，不滥用。

## 语音引擎选择

在 **设置 → 语音引擎** 中选择：

### Apple 原生（推荐）

- **引擎**: macOS SFSpeechRecognizer / SpeechAnalyzer
- **要求**: macOS 12+，无需配置
- **特点**: 零延迟、离线可用、设备端处理，隐私最高
- **限制**: macOS 26+ 才有 SpeechAnalyzer（更高精度）

### WhisperKit

- **引擎**: Apple WhisperKit（本地离线）
- **要求**: 首次使用会自动下载模型（约 1.5 GB）
- **特点**: 精度极高，完全离线，支持更多语言
- **限制**: 下载量较大，首次使用需等待

### DashScope（云端）

- **引擎**: 阿里云百炼 DashScope
- **要求**: 需在 AI 引擎中配置阿里云 API Key
- **特点**: 精度最高，不受本地算力限制
- **限制**: 需要网络连接，有 API 调用成本

## 录音质量优化

- **安静环境** — 降噪算法能处理环境噪底，但近距离清晰的语音效果最好
- **正常语速** — 无需刻意放慢或加快
- **自然停顿** — 去静默算法依赖自然停顿来分割段落，不需要连续不断地说
- **距离适中** — 距离麦克风 20-40cm，和正常对 Mac 说话的距离一致

## 声纹档案

如果你想让谨迹识别不同说话人并自动标记，参考[声纹档案](/docs/guide/speaker-profiles)。

## 相关工作

- [素材导入](/docs/guide/materials) — 文件拖入与文字粘贴
- [AI 编译原理](/docs/guide/ai-compilation) — 了解 AI 如何处理录音内容
