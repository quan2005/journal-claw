---
tags: [journal, showcase, mdx]
summary: 组件收敛项目已完成运行时边界和内容模板重写，下一步是统一手册生成与全量编译验证。
sources:
  [
    src/components/mdx/component-manifest.json,
    .agents/skills/journal/references/component-catalog.md,
    .agents/skills/journal/references/template-examples,
  ]
---

# MDX 组件收敛进展

<Subtitle>一篇用于验证语义组件组合的真实项目日志。</Subtitle>

<StatGroup>
  <Stat label="公共组件" value={61} />
  <Stat label="模板示例" value={104} />
  <Stat label="家族指南" value={9} />
</StatGroup>

<DecisionRecord
  question="如何避免文档、示例和运行时再次漂移"
  decision="manifest 作为组件白名单，skill 示例作为 subtype 正文源"
  options={[
    { label: '继续在生成器内嵌正文', tradeoff: '生成方便，但会形成第二份内容源' },
    { label: '生成器读取 skill 示例', tradeoff: '路径契约更严格，但内容只有一份' },
  ]}
  rationale="运行时、文档和生成输出必须能由自动校验串起来。"
/>

<MilestoneTimeline
  items={[
    { time: 'M1', title: '运行时收敛', desc: '未知组件局部降级，StatGroup 恢复。' },
    { time: 'M2', title: '内容重写', desc: '目录、recipes、9 个指南和 104 个示例更新。' },
    { time: 'M3', title: '生成与验证', desc: '手册、MDX 编译和真实渲染检查。' },
  ]}
/>

<Table
  headers={['风险', '状态', '应对']}
  rows={[
    ['生成器仍含旧正文', 'open', '改为读取 template-examples'],
    ['全组件 demo 仍含退休标签', 'doing', '按 61 项 manifest 重写'],
    ['CSS 真实链路未巡检', 'open', '通过 Journal 详情渲染验证'],
  ]}
/>

<ReferenceList
  sources={[
    {
      path: 'src/components/mdx/component-manifest.json',
      label: '公共组件清单',
      type: 'file',
    },
    {
      path: '.agents/skills/journal/references/component-catalog.md',
      label: '组件使用目录',
      type: 'file',
    },
  ]}
/>
